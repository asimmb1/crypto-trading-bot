[build]
builder = "NIXPACKS"

[deploy]
startCommand = "mkdir -p logs && python3 main.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
healthcheckPath = "/health"
healthcheckTimeout = 30
